basic ticktack toe script
(not very well written since its my first time workign on somthing like this, and therefore had to redo a buch of stuff, leading to slopy repeted code)

the script stores its perameters in a generated pkl file (very buggy only seems to work for one run of the script(though it will cary over to all episodes in that run))
the script is not configured to be trained as its currently configuerd to for running (epsilion is 0 and training loop is replaced by human input options in a shitty cli) 
but the pkl file is included so itl prob work if you try hard engouh

OVERALL
this should just be seen as a demonstration to show that i have worked with reinforcement learning a lil before!!!!!!!!!!!!!!!!
